                                <a href="{{ url_for('edit_booking', bookingid=booking['bookingid']) }}" class="btn btn-primary btn-sm">Edit</a>
                                <form action="{{ url_for('delete_booking', bookingid=booking['bookingid']) }}" method="post" style="display:inline;">
                                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this booking?');">Delete</button>
                                </form>




                                                            <a href="{{ url_for('edit_trainer', trainerid=trainer['trainerid']) }}" class="btn btn-primary btn-sm">Edit</a>

                                <!-- Delete Trainer Button -->
                                <form action="{{ url_for('delete_trainer', trainerid=trainer['trainerid']) }}" method="post" style="display:inline;">
                                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this trainer?');">Delete</button>
                                </form>

                                <a href="{{ url_for('add_trainer') }}" class="btn btn-primary mt-3">Add New Trainer</a>